<Window x:Class="Cryptocurrencies.Views.SearchView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:viewModels="clr-namespace:Cryptocurrencies.ViewModels"
        Title="Search Cryptocurrencies" Height="300" Width="400">

    <Grid Margin="20">
        <StackPanel>
            <TextBlock Text="Search Cryptocurrencies" FontSize="16" Margin="0,0,0,10"/>
            <TextBox Name="SearchTextBox"
                     Width="360" Height="30"
                     Foreground="Gray"
                     GotFocus="TextBox_GotFocus"
                     LostFocus="TextBox_LostFocus"
                     Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"/>

            <ListBox ItemsSource="{Binding SearchResults}" Margin="0,10,0,0">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding Name}" />
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </StackPanel>
    </Grid>
</Window>
