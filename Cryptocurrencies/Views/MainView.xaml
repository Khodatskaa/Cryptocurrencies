<Window x:Class="Cryptocurrencies.Views.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:viewModels="clr-namespace:Cryptocurrencies.ViewModels"
        Title="MainView" Height="450" Width="800">

    <Grid Margin="20">
        <Button Content="Load Data" Command="{Binding LoadDataCommand}" Width="100" Height="30" HorizontalAlignment="Left" VerticalAlignment="Top" />
        <ListBox ItemsSource="{Binding Cryptocurrencies}" Margin="0,40,0,0">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding Name}" FontSize="16" />
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </Grid>
</Window>
